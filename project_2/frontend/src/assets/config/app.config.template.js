window.__env = {
  apiUrl: "$API_URL",
  cognitoLogoutUrl: "$COGNITO_LOGOUT_URL",
  authority: "$COGNITO_AUTHORITY",
  redirectUrl: "$COGNITO_REDIRECT_URL",
  postLogoutRedirectUri: "$COGNITO_POST_LOGOUT_URI",
  clientId: "$COGNITO_CLIENT_ID",
  scope: "$COGNITO_SCOPE",
  responseType: "$COGNITO_RESPONSE_TYPE"
};
